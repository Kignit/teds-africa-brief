# Optional API keys. The app runs without them; connectors that need a key stay
# disabled until set and never fabricate data.
EIA_API_KEY=
FRED_API_KEY=
# UN Comtrade — enriches country profiles with specific export/import products.
# Without it, profiles still carry World Bank oil stance + dollar-debt exposure.
COMTRADE_API_KEY=
