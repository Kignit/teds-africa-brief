dist
coverage
node_modules
package-lock.json
*.example
# prototype reference artifacts (kept for reference, not part of the new app)
*.jsx
data.js
Ted's Africa Brief*.html
Ted's Africa Brief*.md
screenshots

# Vercel parses vercel.json and re-materializes it as compact one-line JSON in
# its build workspace, so `prettier --check .` flags the materialized copy even
# though the committed file is correctly formatted. The committed blob is checked
# strictly and separately via the `format:check:vercel` npm script.
vercel.json

# Generated runtime artifact (a { generatedAt, brief } envelope written by the brief
# generator). It is machine-generated data, not formatted source, so it must not fail
# format:check when the generator commits it.
public/brief.json

# Rolling news-window store: raw source-backed news items the generator persists across
# runs (outside public/, never served, never read by the client). Machine-generated,
# high-churn data — excluded from format:check for the same reason as the brief artifact.
data/news-window.json
